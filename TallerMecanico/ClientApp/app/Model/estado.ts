export class Estado {

    idEstado?: number;
    descripcion?: string;

    constructor(idEstado?: number, descripcion?: string) {
        this.idEstado = idEstado;
        this.descripcion = descripcion;
    }
}